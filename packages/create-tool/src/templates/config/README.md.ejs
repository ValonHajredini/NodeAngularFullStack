# <%= toolName %> Tool

<%= description %>

**Tool ID:** `<%= toolId %>`
**Version:** <%= version %>
**Generated:** <%= new Date().toISOString().split('T')[0] %>

## Features

<% features.forEach(function(feature) { -%>
- <%= feature %>
<% }); -%>

## Installation

This tool has been automatically scaffolded and integrated into the NodeAngularFullStack platform.

### Prerequisites

- PostgreSQL database with `<%= tableName %>` table
- Backend API server running
- Angular frontend application

### Database Setup

Run migrations to create the required database table:

```bash
npm --workspace=apps/api run db:migrate
```

## Usage

### Frontend Component

The <%= toolName %> component is available at:

```
<%= route %>
```

Import and use in your Angular application:

```typescript
import { <%= className %>Component } from './<%= toolId %>.component';
```

### Backend API Endpoints

All endpoints require authentication.

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET    | `<%= apiBase %>` | Get all <%= toolName %> records |
| GET    | `<%= apiBase %>/:id` | Get single <%= toolName %> record |
| POST   | `<%= apiBase %>` | Create new <%= toolName %> record |
| PUT    | `<%= apiBase %>/:id` | Update <%= toolName %> record |
| DELETE | `<%= apiBase %>/:id` | Delete <%= toolName %> record |

### Example API Usage

**Create <%= toolName %>:**

```bash
curl -X POST <%= apiBase %> \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "name": "Example <%= toolName %>",
    "description": "This is an example",
    "createdBy": "USER_UUID"
  }'
```

**Get All <%= toolName %> Records:**

```bash
curl <%= apiBase %> \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Development Notes

### File Structure

```
Frontend:
  apps/web/src/app/features/tools/<%= toolId %>/
  ├── <%= toolId %>.component.ts
  ├── <%= toolId %>.component.html
  ├── <%= toolId %>.component.css
  ├── <%= toolId %>.service.ts
  └── <%= toolId %>.routes.ts

Backend:
  apps/api/src/
  ├── controllers/<%= toolId %>.controller.ts
  ├── services/<%= toolId %>.service.ts
  ├── repositories/<%= toolId %>.repository.ts
  ├── routes/<%= toolId %>.routes.ts
  └── validators/<%= toolId %>.validator.ts

Shared:
  packages/shared/src/types/<%= toolId %>.types.ts
```

### Adding Custom Logic

**Frontend Service:**
- Modify `<%= toolId %>.service.ts` to add custom HTTP methods
- Update `<%= toolId %>.component.ts` to implement UI interactions

**Backend Service:**
- Add business logic validation in `<%= toolId %>.service.ts`
- Implement custom queries in `<%= toolId %>.repository.ts`

**Validation:**
- Extend validation rules in `<%= toolId %>.validator.ts`
- Add custom validators using express-validator

### Testing

**Frontend Tests:**

```bash
npm --workspace=apps/web run test -- --include="**/<%= toolId %>.*.spec.ts"
```

**Backend Tests:**

```bash
npm --workspace=apps/api run test -- --testPathPattern="<%= toolId %>"
```

## Support

For issues or questions about this tool:

1. Check the main project README at `/README.md`
2. Review the architecture documentation in `/docs/architecture/`
3. Consult the project wiki or raise an issue

---

*Generated by @nodeangularfullstack/create-tool CLI*

/**
 * Form Builder Constants
 * Centralized constants for the form builder component to avoid magic numbers.
 */

/** Auto-save interval in milliseconds (30 seconds) */
export const AUTO_SAVE_INTERVAL_MS = 30000;

/** Breakpoint for sidebar responsive behavior in pixels */
export const SIDEBAR_BREAKPOINT_PX = 1024;

/** Default page size for forms pagination */
export const FORMS_PAGE_SIZE = 50;

/** Starting page number for pagination (1-based) */
export const PAGINATION_START_PAGE = 1;
